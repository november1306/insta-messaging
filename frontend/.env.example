# Frontend Environment Variables
# No environment variables needed - frontend uses:
# - Relative URLs (automatically works in dev and production)
# - JWT authentication via session store (no API keys needed)
