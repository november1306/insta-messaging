# Frontend Environment Variables

# API Key for authentication
# Generate a key using: python -m app.cli.generate_api_key --name "Frontend" --type admin --env test
# Or use "demo-token" with USE_STUB_AUTH=true in the backend .env
VITE_API_KEY=demo-token
